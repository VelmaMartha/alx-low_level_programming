#include "main.h"
/**
 *mul - multiplies two integers
 *@a: number to be multiplied
 *@b: number to be multiplied also
 *Return: mult
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
