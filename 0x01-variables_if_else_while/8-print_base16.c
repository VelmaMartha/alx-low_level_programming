#include <stdio.h>
/**
 *main -Entry point
 *
 *Return: Always 0.
 *
 */
int main(void)
{
	int x;
	int m;

	for (x = 48; x <= 57; x++)
	putchar(x);
	{
	for (m = 97; m <= 102; m++)
	putchar(m);
	}
	putchar('\n');
	return (0);
}

